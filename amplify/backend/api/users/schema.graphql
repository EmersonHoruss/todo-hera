type User @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  phone: String!
  age: Int
  address: String
}

type Todo @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  description: String
}
